export default function Header() {
  return (
    <h1
      className="header"
      style={{
        color: "red",
        fontSize: "32px",
        textTransform: "uppercase",
        textAlign: "center",
      }}
    >
      Fast React Pizza Co.
    </h1>
  );
}
